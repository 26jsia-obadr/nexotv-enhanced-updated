services:
  - type: web
    name: nexotv-enhanced
    region: frankfurt
    runtime: docker
    plan: free
    dockerfilePath: ./Dockerfile
    healthCheckPath: /health
    envVars:
      - key: CONFIG_SECRET
        generateValue: true
