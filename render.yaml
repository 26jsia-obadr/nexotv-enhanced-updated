services:
  - type: web
    name: nexotv-enhanced
    runtime: docker
    plan: starter
    dockerfilePath: ./Dockerfile
    healthCheckPath: /health
    envVars:
      - key: CONFIG_SECRET
        generateValue: true
    disk:
      name: nexotv-data
      mountPath: /app/data
      sizeGB: 1
