services:
  - type: web
    name: nexotv-enhanced
    runtime: docker
    plan: free
    dockerfilePath: ./Dockerfile
    healthCheckPath: /health
    envVars:
      - key: CONFIG_SECRET
        generateValue: true
